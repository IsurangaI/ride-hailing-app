package com.ridehailing.booking_service.constants;

public enum RideStatus {
        PENDING,
        OFFERING,
        ACCEPTED,
        IN_PROGRESS,
        COMPLETED,
        CANCELLED,
        REJECTED
}
